language: python

cache: pip

python:
    - "2.7"

before_install:
    - pip install pytest pytest-cov
    - pip install coveralls

install:
    - python setup.py install

script:
    - py.test 

after_success:
    - coveralls
